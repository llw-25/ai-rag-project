<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wanghui.airagproject.dao.DocDao">

    <insert id="insert">
        INSERT INTO doc_chunk(content,create_time)
        VALUES (#{content},now())
    </insert>

</mapper>