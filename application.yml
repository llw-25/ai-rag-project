server:
  port: 8080

servlet:
  context-path: /

multipart:
  max-file-size: 10MB
  max-request-size: 10MB

spring:
  profiles:
    active: local

ai:
  chat:
    # Chat configuration properties
  embedding:
    # Embedding configuration properties
